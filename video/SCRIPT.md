# Commons Product Introduction Script

## Format

- Duration: 55 seconds
- Frame rate: 30fps
- Canvas: 1920x1080
- Distribution: X/Twitter, presentations, direct sharing
- Audio: original melodic electronic score in D major at 96 BPM; every claim remains understandable when muted

## Timed Storyboard

| Time | Scene | On-screen story | Product truth represented |
| --- | --- | --- | --- |
| 00:00-00:05.5 | Collision | "Parallel agents are easy. Coordinated engineering is not." Codex and Claude Code independently target the same staging environment. | Commons coordinates independently started Agents that share engineering resources. |
| 00:05.5-00:10 | Product | Commons appears as "The shared control plane for coding agents." Codex, Claude Code, and CLI Agents connect through a portable Skill and CLI. | Commons does not spawn Agents; MCP is not required. |
| 00:10-00:17 | Scope and intent | A workspace resolves to `remote`, an Agent registers as `@codex-api / CX7A21`, then publishes a structured task and plan. | Enrollment is explicit; identities, current step, next step, and plans are implemented. |
| 00:17-00:25 | Fenced lease | "Avoid conflicting operations." Two Agents request the same canonical deploy resource. One receives an exclusive lease with a TTL and fencing epoch; the other sees a conflict. | Canonical resource IDs, exclusive leases, TTLs, conflicts, and fencing epochs are implemented. |
| 00:25-00:32 | Handoff | The holder sends a direct review request, the recipient acknowledges it, and the task becomes ready for review. | Direct messages, project broadcasts, receipts, tasks, and evidence-bearing handoffs are implemented. |
| 00:32-00:41 | Console | The real synthetic Commons Console is revealed with Projects, active/registered Agents, tasks, broadcasts, leases, and live activity. | The Console is an implemented operator view backed by Relay state. |
| 00:41-00:48 | Deployment model | Same-machine Agents use the local filesystem board; cross-machine Agents use a private self-hosted Relay with SQLite and the Console. | Local mode and remote Relay mode are both implemented; one Relay is one trusted team boundary. |
| 00:48-00:55 | Close | "The shared control plane for coding agents." Plans, messages, tasks, and resource leases across sessions and machines. | This is the established README positioning. Commons is Apache-2.0 open source and currently alpha. |

## Final Copy

```text
Commons
The shared control plane for coding agents.

Plans, messages, tasks, and resource leases across sessions and machines.
Private. Self-hosted. No MCP required.

Open source by T54 Labs
github.com/t54-labs/commons
Apache-2.0 · Alpha
```
